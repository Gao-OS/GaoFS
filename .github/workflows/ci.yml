name: ci

on:
  pull_request:
  push:
    branches: [ work, main, master ]

jobs:
  zig:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v1
        with:
          version: 0.12.0
      - run: zig build
      - run: zig build test
